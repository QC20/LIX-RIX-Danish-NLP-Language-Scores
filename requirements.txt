# Core dependencies for the Lix/Rix readability calculator.
# Install everything with:
#   pip install -r requirements.txt
#
# Then download the spaCy language model(s) you need:
#   python -m spacy download da_core_news_sm   # Danish (default)
#   python -m spacy download sv_core_news_sm   # Swedish
#   python -m spacy download nb_core_news_sm   # Norwegian Bokmål
#   python -m spacy download nl_core_news_sm   # Dutch

PyPDF2>=3.0.0
python-docx>=1.0.0
spacy>=3.7.0
pandas>=2.0.0
openpyxl>=3.1.0
tqdm>=4.65.0